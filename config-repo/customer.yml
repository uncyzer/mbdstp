spring:
  application:
    name: customer
  datasource:
    url: jdbc:h2:mem:customer
  h2:
    console:
      enabled: true
server:
  port: 8080
management:
  endpoints:
    web:
      exposure:
        include: "*"